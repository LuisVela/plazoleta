server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:33061/plazoleta
    username: root
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
appDescription: "Pragma Powerup"
appVersion: "1.0.0"
